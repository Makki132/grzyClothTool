<UserControl x:Class="grzyClothTool.Views.ProjectWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:grzyClothTool.Views" 
             xmlns:c="clr-namespace:grzyClothTool.Controls"
             xmlns:m="clr-namespace:grzyClothTool.Models"
             xmlns:icons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             d:DataContext="{d:DesignInstance Type=m:AddonManagerDesign, IsDesignTimeCreatable=True}"
             Background="{DynamicResource Brush50}"
             d:Width="1200"
             d:Height="600"
             d:Background="white"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <!-- Left Panel with Drawables List -->
        <Grid Grid.Column="0" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <TextBlock Grid.Row="0" Text="Drawables" FontSize="16" FontWeight="SemiBold" 
                       Foreground="{DynamicResource Brush950}" Margin="8,8,8,4" />
            
            <!-- Search Box -->
            <c:SearchBox x:Name="drawableSearchBox" Grid.Row="1" 
                         PlaceholderText="Enter search term (CTRL+F)" 
                         Margin="8,4,8,8"
                         SearchTextChanged="SearchBox_TextChanged" />
            
            <!-- TabControl for Addons -->
            <TabControl Grid.Row="2" ItemsSource="{Binding Addons}" SelectedIndex="0" 
                        SelectionChanged="TabControl_SelectionChanged" 
                        Background="{DynamicResource Brush50}" 
                        BorderBrush="{DynamicResource Brush300}"
                        Margin="0">
                <TabControl.Resources>
                    <!-- Style for the Tab Headers -->
                    <Style TargetType="{x:Type TabItem}">
                        <Setter Property="Foreground" Value="{DynamicResource Brush950}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TabItem}">
                                    <Grid>
                                        <Border 
                                            Name="Border"
                                            Margin="0,0,-4,0"
                                            Background="{DynamicResource Brush200}"
                                            BorderBrush="{DynamicResource Brush300}" 
                                            BorderThickness="1,1,1,0" >
                                            <ContentPresenter x:Name="ContentSite"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"
                                                ContentSource="Header"
                                                Margin="12,2,12,2"
                                                RecognizesAccessKey="True"/>
                                        </Border>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="FontWeight" Value="Bold" />
                                            <Setter Property="Panel.ZIndex" Value="100" />
                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource Brush50}" />
                                            <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource Brush50}" BorderThickness="0" Padding="4">
                            <c:DrawableList x:Name="drawableList" ItemsSource="{Binding Drawables, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                            DrawableListKeyDown="SelectedDrawable_KeyDown" 
                                            DrawableListSelectedValueChanged="SelectedDrawable_Changed" />
                        </Border>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Grid>

        <GroupBox Style="{StaticResource FixedGroupBox}" Grid.Column="1" Grid.Row="0" Margin="2" Header="Selected drawable options">
            <c:SelectedDrawable x:Name="SelDrawable" TextureListSelectedValueChanged="SelectedDrawable_TextureChanged" SelectedDrawableUpdated="SelectedDrawable_Updated" SelectedDraw="{Binding SelectedAddon.SelectedDrawable}" SelectedTxt="{Binding SelectedAddon.SelectedTexture}" />
        </GroupBox>

        <!-- Bottom Action Bar -->
        <Grid Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Background="{DynamicResource Brush100}" MinHeight="36">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Import Package Button -->
            <Button Grid.Column="0" Click="ImportPackage_Click" Style="{StaticResource ModernButton}" 
                    Padding="8,4" Margin="4,0,4,0" ToolTip="Import Package">
                <StackPanel Orientation="Horizontal">
                    <icons:MaterialIcon Kind="FolderOpen" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,4,0" />
                    <TextBlock Text="Import package" VerticalAlignment="Center" FontSize="12" />
                </StackPanel>
            </Button>

            <!-- Add Button -->
            <Button Grid.Column="1" Click="Add_Click" Style="{StaticResource ModernButton}" 
                    Padding="8,4" Margin="0,0,4,0" ToolTip="Add">
                <StackPanel Orientation="Horizontal">
                    <icons:MaterialIcon Kind="Plus" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,4,0" />
                    <TextBlock Text="Add" VerticalAlignment="Center" FontSize="12" />
                </StackPanel>
            </Button>

            <!-- Add Male Button with Dropdown -->
            <c:CustomButton Grid.Column="2" Label="Add Male" Tag="MALE"
                            Padding="8,6" Margin="4,4,4,4" Width="120" CornerRadius="3" FontSize="13" VerticalAlignment="Stretch" DropdownEnabled="True">
                <c:CustomButton.DropdownContent>
                    <StackPanel>
                        <c:CustomButton Tag="MALE" Label="Add file(s)" CornerRadius="3" Margin="2" Padding="12,6" MinHeight="28" FontSize="13" MyBtnClickEvent="Add_DrawableFile" />
                        <c:CustomButton Tag="MALE" Label="Add folder" CornerRadius="3" Margin="2" Padding="12,6" MinHeight="28" FontSize="13" MyBtnClickEvent="Add_DrawableFolder" />
                    </StackPanel>
                </c:CustomButton.DropdownContent>
            </c:CustomButton>

            <!-- Add Female Button with Dropdown -->
            <c:CustomButton Grid.Column="3" Label="Add Female" Tag="FEMALE"
                            Padding="8,6" Margin="4,4,4,4" Width="130" CornerRadius="3" FontSize="13" VerticalAlignment="Stretch" DropdownEnabled="True">
                <c:CustomButton.DropdownContent>
                    <StackPanel>
                        <c:CustomButton Tag="FEMALE" Label="Add file(s)" CornerRadius="3" Margin="2" Padding="12,6" MinHeight="28" FontSize="13" MyBtnClickEvent="Add_DrawableFile" />
                        <c:CustomButton Tag="FEMALE" Label="Add folder" CornerRadius="3" Margin="2" Padding="12,6" MinHeight="28" FontSize="13" MyBtnClickEvent="Add_DrawableFolder" />
                    </StackPanel>
                </c:CustomButton.DropdownContent>
            </c:CustomButton>

            <!-- Remove Button -->
            <Button Grid.Column="4" Click="Delete_SelectedDrawable" Style="{StaticResource ModernButton}" 
                    Padding="8,4" Margin="0,0,4,0" ToolTip="Remove">
                <StackPanel Orientation="Horizontal">
                    <icons:MaterialIcon Kind="TrashCanOutline" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,4,0" />
                    <TextBlock Text="Remove" VerticalAlignment="Center" FontSize="12" />
                </StackPanel>
            </Button>
        </Grid>
        
    </Grid>
</UserControl>
